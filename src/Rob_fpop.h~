#ifndef ROB_FPOPH
#define ROB_FPOPH

#include "liste.h"

void rob_fpop(const double *profil, const int *nbi, const double *lambda_, const double *threshold_,
		const double *mini, const double *maxi, int *origine, double *cout_n, double *mean_n);

#endif
